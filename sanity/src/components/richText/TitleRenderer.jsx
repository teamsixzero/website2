/* eslint-disable react/prop-types */
import React from 'react'

const TitleRenderer = ({children}) => (
  <span style={{fontSize: '2.5rem', fontWeight: '700'}}>{children}</span>
)

export default TitleRenderer
